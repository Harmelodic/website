apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: website
  annotations:
    kubernetes.io/tls-acme: "true"
    cert-manager.io/issuer: letsencrypt # Will generate Certificate
spec:
  ingressClassName: public
  rules:
    - host: harmelodic.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  name: http
    - host: "*.harmelodic.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  name: http
  tls:
    - hosts:
        - "*.harmelodic.com"
        - harmelodic.com
      secretName: tls-cert-wildcard-harmelodic-com # Secret will be auto-generated by cert-manager
