apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: main
  namespace: {{.KUBE_NAMESPACE}}
  labels:
    app: main
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
    certmanager.k8s.io/issuer: letsencrypt-staging
    certmanager.k8s.io/acme-dns01-provider: route53
    certmanager.k8s.io/acme-challenge-type: dns01
spec:
  rules:
  - host: harmelodic.com
    http:
      paths:
        - path: /
          backend:
            serviceName: main
            servicePort: 80
  - host: "*.harmelodic.com"
    http:
      paths:
        - path: /
          backend:
            serviceName: main
            servicePort: 80
  tls:
  - hosts:
    - "*.harmelodic.com"
    secretName: tls-staging-cert-wildcard-harmelodic
